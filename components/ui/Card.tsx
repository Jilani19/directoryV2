import React from "react"; import { cva, type VariantProps } from "class-variance-authority"; import { cn } from "../../lib/cn"; const cardVariants = cva("rounded-2xl text-card-foreground", { variants: { variant: { default: "bg-card shadow", premium: "bg-gradient-to-br from-white to-muted shadow-lg border border-primary/10", glass: "bg-white/70 backdrop-blur-md border border-white/20 shadow-glass", border: "border border-border bg-card", hoverLift: "bg-card shadow border border-transparent transition-all duration-300 hover:-translate-y-1 hover:shadow-lg hover:border-primary/20" } }, defaultVariants: { variant: "default" } }); export interface CardProps extends React.HTMLAttributes<HTMLDivElement>, VariantProps<typeof cardVariants> {} export const Card = React.forwardRef<HTMLDivElement, CardProps>(({ className, variant, ...props }, ref) => ( <div ref={ref} className={cn(cardVariants({ variant, className }))} {...props} /> )); Card.displayName = "Card";

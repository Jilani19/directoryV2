import React from "react"; import { cn } from "../../lib/cn"; export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> { label?: string; error?: string; helperText?: string; leftIcon?: React.ReactNode; rightIcon?: React.ReactNode; } export const Input = React.forwardRef<HTMLInputElement, InputProps>(({ className, label, error, helperText, leftIcon, rightIcon, id, ...props }, ref) => { const reactId = React.useId(); const generatedId = id || reactId; return ( <div className="w-full flex flex-col gap-1.5"> {label && <label htmlFor={generatedId} className="text-sm font-medium text-foreground">{label}</label>} <div className={cn("relative flex items-center rounded-md border bg-transparent px-3 py-2 text-sm shadow-sm transition-colors focus-within:ring-1 focus-within:ring-ring focus-within:border-primary", error ? "border-danger focus-within:ring-danger focus-within:border-danger" : "border-input", className)}> {leftIcon && <span className="mr-2 text-muted-foreground">{leftIcon}</span>} <input id={generatedId} ref={ref} className="flex-1 w-full bg-transparent outline-none placeholder:text-muted-foreground disabled:cursor-not-allowed disabled:opacity-50" {...props} /> {rightIcon && <span className="ml-2 text-muted-foreground">{rightIcon}</span>} </div> {error && <p className="text-xs text-danger">{error}</p>} {helperText && !error && <p className="text-xs text-muted-foreground">{helperText}</p>} </div> ); }); Input.displayName = "Input";


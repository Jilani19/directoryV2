import React from "react"; import { Container } from "./Container"; import { cn } from "../../lib/cn"; export interface PageHeaderProps extends React.HTMLAttributes<HTMLDivElement> { title: string; subtitle?: string; actions?: React.ReactNode; } export function PageHeader({ title, subtitle, actions, className, ...props }: PageHeaderProps) { return ( <div className={cn("w-full bg-navy text-white pt-32 pb-16 md:pt-40 md:pb-20", className)} {...props}> <Container> <div className="flex flex-col md:flex-row md:items-end justify-between gap-6"> <div className="flex flex-col gap-4 max-w-3xl"> <h1 className="text-4xl md:text-5xl lg:text-6xl font-extrabold tracking-tight leading-tight">{title}</h1> {subtitle && <p className="text-lg md:text-xl text-navy-200">{subtitle}</p>} </div> {actions && <div>{actions}</div>} </div> </Container> </div> ); }

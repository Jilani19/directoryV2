import { create } from "zustand"; interface UiState { isSidebarOpen: boolean; isModalOpen: boolean; toggleSidebar: () => void; setModalOpen: (isOpen: boolean) => void; } export const useUiStore = create<UiState>((set) => ({ isSidebarOpen: false, isModalOpen: false, toggleSidebar: () => set((state) => ({ isSidebarOpen: !state.isSidebarOpen })), setModalOpen: (isOpen) => set({ isModalOpen: isOpen }) }));
